# Gateway (server)
# Full invoke URL, e.g. http://localhost:3000/agent/shop/v1/invoke
PIVOTA_AGENT_URL=

# Optional auth (server only). Do NOT set these as NEXT_PUBLIC_* in production.
PIVOTA_AGENT_API_KEY=
AGENT_API_KEY=
SHOP_GATEWAY_AGENT_API_KEY=

# Checkout providers (client)
# Stripe (optional)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Adyen (optional)
NEXT_PUBLIC_ADYEN_CLIENT_KEY=
NEXT_PUBLIC_ADYEN_ENVIRONMENT=test
